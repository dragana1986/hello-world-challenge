apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}          # must match the Deployment's pod labels
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}   # hint: targetPort (8080)